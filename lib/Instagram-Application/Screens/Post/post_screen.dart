import 'package:flutter/material.dart';

class InstaPostScreen extends StatelessWidget {
  const InstaPostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Post')));
  }
}
