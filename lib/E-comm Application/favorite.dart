import 'package:flutter/material.dart';

class EcommFavorite extends StatelessWidget {
  const EcommFavorite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
