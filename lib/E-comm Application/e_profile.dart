import 'package:flutter/material.dart';

class EcommProfile extends StatelessWidget {
  const EcommProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
