import 'package:flutter/cupertino.dart';

class Status1Screen extends StatelessWidget {
  const Status1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("This is Status Screen"));
  }
}
